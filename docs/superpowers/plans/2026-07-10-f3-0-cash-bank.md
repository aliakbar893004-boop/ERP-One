# Fase 3-0 — Finance Foundation + Cash/Bank Master Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Add a `Finance` menu group and a `CashBankAccount` master (CRUD) as the foundation for AP/AR/Expense.

**Architecture:** Same layering as the rest of ErpOne — Domain entity → EF config inline in `AppDbContext` → `ErpOne.Application/CashBank` (service + DTOs + validators) → `ErpOne.Infrastructure/Services` impl → Blazor `.pi`/`.cf` pages. Permissions auto-derive from `AppMenus`; NavMenu is now data-driven so a new `Finance` group appears automatically. Mirrors the just-built `Currency` master.

**Tech Stack:** .NET / C#, EF Core (SQL Server prod, SQLite in-memory tests), Blazor Server, FluentValidation, xUnit.

## Global Constraints
- UI English; index `.pi`, form `.cf` (Atlas).
- Register new entity in `tablePrefixes` (`M_`) in `AppDbContext` or model build fails.
- `CashBankAccount.Currency` stored as `string(3)` currency Code (dropdown from Currency master) — no FK.
- Integration tests use `EnsureCreated()` (no migration needed for tests); migration still generated for prod.
- Commit after each task's tests pass.

---

## Task 1: CashBankAccount entity + EF config + seed + migration

**Files:**
- Create: `src/ErpOne.Domain/Entities/CashBankType.cs`
- Create: `src/ErpOne.Domain/Entities/CashBankAccount.cs`
- Modify: `src/ErpOne.Infrastructure/Persistence/AppDbContext.cs`
- Create: migration `AddCashBankAccount` (generated)

**Interfaces:**
- Produces: `enum CashBankType { Cash, Bank }`; `CashBankAccount` with ctor `(string code, string name, CashBankType type, string currency, decimal openingBalance, string? bankName, string? accountNumber, string? accountHolder, bool isActive)` + `Update(...)` same params; props `Id, Code, Name, Type, Currency, OpeningBalance, BankName, AccountNumber, AccountHolder, IsActive`. `AppDbContext.CashBankAccounts`.

- [ ] **Step 1: Create the enum**

`src/ErpOne.Domain/Entities/CashBankType.cs`:
```csharp
namespace ErpOne.Domain.Entities;

/// <summary>Jenis akun finansial.</summary>
public enum CashBankType { Cash, Bank }
```

- [ ] **Step 2: Create the entity**

`src/ErpOne.Domain/Entities/CashBankAccount.cs`:
```csharp
using ErpOne.Domain.Common;

namespace ErpOne.Domain.Entities;

/// <summary>Akun kas atau bank (master). Saldo berjalan dihitung dari mutasi (AP/AR/Expense).</summary>
public class CashBankAccount : AuditableEntity
{
    public int Id { get; private set; }
    public string Code { get; private set; } = default!;
    public string Name { get; private set; } = default!;
    public CashBankType Type { get; private set; }
    public string Currency { get; private set; } = "IDR";
    public decimal OpeningBalance { get; private set; }
    public string? BankName { get; private set; }
    public string? AccountNumber { get; private set; }
    public string? AccountHolder { get; private set; }
    public bool IsActive { get; private set; }

    private CashBankAccount() { } // EF Core

    public CashBankAccount(string code, string name, CashBankType type, string currency,
        decimal openingBalance, string? bankName, string? accountNumber, string? accountHolder, bool isActive)
        => Set(code, name, type, currency, openingBalance, bankName, accountNumber, accountHolder, isActive);

    public void Update(string code, string name, CashBankType type, string currency,
        decimal openingBalance, string? bankName, string? accountNumber, string? accountHolder, bool isActive)
        => Set(code, name, type, currency, openingBalance, bankName, accountNumber, accountHolder, isActive);

    private void Set(string code, string name, CashBankType type, string currency,
        decimal openingBalance, string? bankName, string? accountNumber, string? accountHolder, bool isActive)
    {
        if (string.IsNullOrWhiteSpace(code)) throw new ArgumentException("Code is required.", nameof(code));
        if (string.IsNullOrWhiteSpace(name)) throw new ArgumentException("Name is required.", nameof(name));
        if (openingBalance < 0) throw new ArgumentException("Opening balance cannot be negative.", nameof(openingBalance));

        Code = code.Trim().ToUpperInvariant();
        Name = name.Trim();
        Type = type;
        Currency = string.IsNullOrWhiteSpace(currency) ? "IDR" : currency.Trim().ToUpperInvariant();
        OpeningBalance = openingBalance;
        BankName = Clean(bankName);
        AccountNumber = Clean(accountNumber);
        AccountHolder = Clean(accountHolder);
        IsActive = isActive;
    }

    private static string? Clean(string? v) => string.IsNullOrWhiteSpace(v) ? null : v.Trim();
}
```

- [ ] **Step 3: Register DbSet, config, seed, table prefix**

In `AppDbContext.cs`, add DbSet near the master DbSets (after `Currencies`):
```csharp
    public DbSet<CashBankAccount> CashBankAccounts => Set<CashBankAccount>();
```

Add config inside `OnModelCreating` (near the other master configs, e.g. after `Currency`):
```csharp
        modelBuilder.Entity<CashBankAccount>(e =>
        {
            e.HasKey(x => x.Id);
            e.Property(x => x.Code).HasMaxLength(20).IsRequired();
            e.HasIndex(x => x.Code).IsUnique();
            e.Property(x => x.Name).HasMaxLength(100).IsRequired();
            e.Property(x => x.Type).HasConversion<string>().HasMaxLength(20).IsRequired();
            e.Property(x => x.Currency).HasMaxLength(3).IsRequired();
            e.Property(x => x.OpeningBalance).HasPrecision(18, 2);
            e.Property(x => x.BankName).HasMaxLength(100);
            e.Property(x => x.AccountNumber).HasMaxLength(50);
            e.Property(x => x.AccountHolder).HasMaxLength(100);

            e.HasData(new
            {
                Id = 1,
                Code = "CASH",
                Name = "Main Cash",
                Type = CashBankType.Cash,
                Currency = "IDR",
                OpeningBalance = 0m,
                IsActive = true,
                CreatedAt = new DateTime(2026, 1, 1, 0, 0, 0, DateTimeKind.Utc),
                CreatedBy = (string?)"system"
            });
        });
```

Add to `tablePrefixes` (Master section):
```csharp
            [nameof(CashBankAccount)] = "M_",
```

- [ ] **Step 4: Build**

Run: `dotnet build`
Expected: Build succeeded.

- [ ] **Step 5: Generate migration**

Run:
```bash
dotnet ef migrations add AddCashBankAccount --project src/ErpOne.Infrastructure --startup-project src/ErpOne.Web
```
Expected: Done. Inspect `Up()` creates `M_CashBankAccounts` with the seed row + unique index on Code.

- [ ] **Step 6: Commit**

```bash
git add src/ErpOne.Domain/Entities/CashBankType.cs src/ErpOne.Domain/Entities/CashBankAccount.cs src/ErpOne.Infrastructure/Persistence/AppDbContext.cs src/ErpOne.Infrastructure/Persistence/Migrations/
git commit -m "feat: add CashBankAccount entity, config, seed, and migration"
```

---

## Task 2: CashBankAccountService + DTOs + validators + DI + tests

**Files:**
- Create: `src/ErpOne.Application/CashBank/CashBankAccountDtos.cs`
- Create: `src/ErpOne.Application/CashBank/ICashBankAccountService.cs`
- Create: `src/ErpOne.Application/CashBank/CashBankAccountValidators.cs`
- Create: `src/ErpOne.Infrastructure/Services/CashBankAccountService.cs`
- Modify: `src/ErpOne.Infrastructure/DependencyInjection.cs`
- Test: `tests/ErpOne.IntegrationTests/CashBankAccountServiceTests.cs`

**Interfaces:**
- Consumes: `CashBankAccount`, `CashBankType` (Task 1).
- Produces:
  - `record CashBankAccountDto(int Id, string Code, string Name, string Type, string Currency, decimal OpeningBalance, string? BankName, string? AccountNumber, string? AccountHolder, bool IsActive, DateTime CreatedAt, string? CreatedBy)`
  - `record CreateCashBankAccountRequest(string Code, string Name, string Type, string Currency, decimal OpeningBalance, string? BankName, string? AccountNumber, string? AccountHolder, bool IsActive)`
  - `record UpdateCashBankAccountRequest(...)` identical fields
  - `ICashBankAccountService` with `GetAllAsync`, `GetActiveAsync`, `GetPagedAsync`, `GetByIdAsync`, `CreateAsync`, `UpdateAsync`, `DeleteAsync`.

- [ ] **Step 1: Write DTOs, interface, validators**

`src/ErpOne.Application/CashBank/CashBankAccountDtos.cs`:
```csharp
namespace ErpOne.Application.CashBank;

public record CashBankAccountDto(int Id, string Code, string Name, string Type, string Currency, decimal OpeningBalance, string? BankName, string? AccountNumber, string? AccountHolder, bool IsActive, DateTime CreatedAt, string? CreatedBy);
public record CreateCashBankAccountRequest(string Code, string Name, string Type, string Currency, decimal OpeningBalance, string? BankName, string? AccountNumber, string? AccountHolder, bool IsActive);
public record UpdateCashBankAccountRequest(string Code, string Name, string Type, string Currency, decimal OpeningBalance, string? BankName, string? AccountNumber, string? AccountHolder, bool IsActive);
```

`src/ErpOne.Application/CashBank/ICashBankAccountService.cs`:
```csharp
using ErpOne.Application.Common;

namespace ErpOne.Application.CashBank;

public interface ICashBankAccountService
{
    Task<IReadOnlyList<CashBankAccountDto>> GetAllAsync(CancellationToken ct = default);
    Task<IReadOnlyList<CashBankAccountDto>> GetActiveAsync(CancellationToken ct = default);
    Task<PagedResult<CashBankAccountDto>> GetPagedAsync(int page, int pageSize, string? search = null, CancellationToken ct = default);
    Task<CashBankAccountDto?> GetByIdAsync(int id, CancellationToken ct = default);
    Task<CashBankAccountDto> CreateAsync(CreateCashBankAccountRequest request, CancellationToken ct = default);
    Task<bool> UpdateAsync(int id, UpdateCashBankAccountRequest request, CancellationToken ct = default);
    Task<bool> DeleteAsync(int id, CancellationToken ct = default);
}
```

`src/ErpOne.Application/CashBank/CashBankAccountValidators.cs`:
```csharp
using FluentValidation;

namespace ErpOne.Application.CashBank;

public class CreateCashBankAccountValidator : AbstractValidator<CreateCashBankAccountRequest>
{
    public CreateCashBankAccountValidator()
    {
        RuleFor(x => x.Code).NotEmpty().MaximumLength(20)
            .Matches("^[A-Za-z0-9-]+$").WithMessage("Code may only contain letters, numbers, and dashes.");
        RuleFor(x => x.Name).NotEmpty().MaximumLength(100);
        RuleFor(x => x.Type).Must(t => t is "Cash" or "Bank").WithMessage("Type must be Cash or Bank.");
        RuleFor(x => x.Currency).NotEmpty().Length(3);
        RuleFor(x => x.OpeningBalance).GreaterThanOrEqualTo(0);
        RuleFor(x => x.BankName).MaximumLength(100);
        RuleFor(x => x.AccountNumber).MaximumLength(50);
        RuleFor(x => x.AccountHolder).MaximumLength(100);
    }
}

public class UpdateCashBankAccountValidator : AbstractValidator<UpdateCashBankAccountRequest>
{
    public UpdateCashBankAccountValidator()
    {
        RuleFor(x => x.Code).NotEmpty().MaximumLength(20)
            .Matches("^[A-Za-z0-9-]+$").WithMessage("Code may only contain letters, numbers, and dashes.");
        RuleFor(x => x.Name).NotEmpty().MaximumLength(100);
        RuleFor(x => x.Type).Must(t => t is "Cash" or "Bank").WithMessage("Type must be Cash or Bank.");
        RuleFor(x => x.Currency).NotEmpty().Length(3);
        RuleFor(x => x.OpeningBalance).GreaterThanOrEqualTo(0);
        RuleFor(x => x.BankName).MaximumLength(100);
        RuleFor(x => x.AccountNumber).MaximumLength(50);
        RuleFor(x => x.AccountHolder).MaximumLength(100);
    }
}
```

- [ ] **Step 2: Write the failing tests**

`tests/ErpOne.IntegrationTests/CashBankAccountServiceTests.cs`:
```csharp
using Microsoft.Extensions.DependencyInjection;
using ErpOne.Application.CashBank;
using FluentValidation;
using Xunit;

namespace ErpOne.IntegrationTests;

public class CashBankAccountServiceTests : IClassFixture<CustomWebApplicationFactory>
{
    private readonly CustomWebApplicationFactory _factory;
    public CashBankAccountServiceTests(CustomWebApplicationFactory factory)
    {
        _factory = factory;
        _factory.InitializeDatabase();
    }

    [Fact]
    public async Task Seeded_default_cash_account_exists()
    {
        using var scope = _factory.Services.CreateScope();
        var svc = scope.ServiceProvider.GetRequiredService<ICashBankAccountService>();
        var all = await svc.GetAllAsync();
        Assert.Contains(all, a => a.Code == "CASH" && a.Type == "Cash");
    }

    [Fact]
    public async Task Create_bank_account_persists_bank_fields_and_normalizes_code()
    {
        using var scope = _factory.Services.CreateScope();
        var svc = scope.ServiceProvider.GetRequiredService<ICashBankAccountService>();

        var created = await svc.CreateAsync(new CreateCashBankAccountRequest(
            "bca-01", "BCA Operational", "Bank", "IDR", 1000m, "BCA", "1234567890", "PT ERP One", true));

        Assert.Equal("BCA-01", created.Code);
        Assert.Equal("Bank", created.Type);
        Assert.Equal("1234567890", created.AccountNumber);

        var fetched = await svc.GetByIdAsync(created.Id);
        Assert.Equal("BCA", fetched!.BankName);
    }

    [Fact]
    public async Task Create_duplicate_code_throws()
    {
        using var scope = _factory.Services.CreateScope();
        var svc = scope.ServiceProvider.GetRequiredService<ICashBankAccountService>();

        await svc.CreateAsync(new CreateCashBankAccountRequest("DUP", "First", "Cash", "IDR", 0m, null, null, null, true));
        await Assert.ThrowsAsync<ValidationException>(
            () => svc.CreateAsync(new CreateCashBankAccountRequest("dup", "Second", "Cash", "IDR", 0m, null, null, null, true)));
    }

    [Fact]
    public async Task GetActive_excludes_inactive()
    {
        using var scope = _factory.Services.CreateScope();
        var svc = scope.ServiceProvider.GetRequiredService<ICashBankAccountService>();

        var inactive = await svc.CreateAsync(new CreateCashBankAccountRequest("INACT", "Closed", "Cash", "IDR", 0m, null, null, null, false));
        var active = await svc.GetActiveAsync();
        Assert.DoesNotContain(active, a => a.Id == inactive.Id);
    }
}
```

- [ ] **Step 3: Run tests to verify they fail**

Run: `dotnet test tests/ErpOne.IntegrationTests --filter CashBankAccountServiceTests`
Expected: FAIL — `ICashBankAccountService` not registered.

- [ ] **Step 4: Implement the service**

`src/ErpOne.Infrastructure/Services/CashBankAccountService.cs`:
```csharp
using FluentValidation;
using Microsoft.EntityFrameworkCore;
using ErpOne.Application.CashBank;
using ErpOne.Application.Common;
using ErpOne.Domain.Entities;
using ErpOne.Infrastructure.Persistence;

namespace ErpOne.Infrastructure.Services;

public class CashBankAccountService(
    AppDbContext db,
    IValidator<CreateCashBankAccountRequest> createValidator,
    IValidator<UpdateCashBankAccountRequest> updateValidator) : ICashBankAccountService
{
    public async Task<IReadOnlyList<CashBankAccountDto>> GetAllAsync(CancellationToken ct = default) =>
        await db.CashBankAccounts.AsNoTracking().OrderBy(x => x.Code).Select(x => ToDto(x)).ToListAsync(ct);

    public async Task<IReadOnlyList<CashBankAccountDto>> GetActiveAsync(CancellationToken ct = default) =>
        await db.CashBankAccounts.AsNoTracking().Where(x => x.IsActive).OrderBy(x => x.Code).Select(x => ToDto(x)).ToListAsync(ct);

    public async Task<PagedResult<CashBankAccountDto>> GetPagedAsync(int page, int pageSize, string? search = null, CancellationToken ct = default)
    {
        page = Math.Max(1, page);
        pageSize = Math.Clamp(pageSize, 1, 200);

        var query = db.CashBankAccounts.AsNoTracking();
        if (!string.IsNullOrWhiteSpace(search))
            query = query.Where(x => x.Code.Contains(search) || x.Name.Contains(search));

        var total = await query.CountAsync(ct);
        var items = await query.OrderBy(x => x.Code)
            .Skip((page - 1) * pageSize).Take(pageSize)
            .Select(x => ToDto(x)).ToListAsync(ct);

        return new PagedResult<CashBankAccountDto>(items, total, page, pageSize);
    }

    public async Task<CashBankAccountDto?> GetByIdAsync(int id, CancellationToken ct = default)
    {
        var x = await db.CashBankAccounts.AsNoTracking().FirstOrDefaultAsync(e => e.Id == id, ct);
        return x is null ? null : ToDto(x);
    }

    public async Task<CashBankAccountDto> CreateAsync(CreateCashBankAccountRequest request, CancellationToken ct = default)
    {
        await createValidator.ValidateAndThrowAsync(request, ct);
        var code = request.Code.Trim().ToUpperInvariant();
        await EnsureCodeUniqueAsync(code, null, ct);

        var entity = new CashBankAccount(code, request.Name, ParseType(request.Type), request.Currency,
            request.OpeningBalance, request.BankName, request.AccountNumber, request.AccountHolder, request.IsActive);
        db.CashBankAccounts.Add(entity);
        await db.SaveChangesAsync(ct);
        return ToDto(entity);
    }

    public async Task<bool> UpdateAsync(int id, UpdateCashBankAccountRequest request, CancellationToken ct = default)
    {
        await updateValidator.ValidateAndThrowAsync(request, ct);

        var entity = await db.CashBankAccounts.FirstOrDefaultAsync(e => e.Id == id, ct);
        if (entity is null) return false;

        var code = request.Code.Trim().ToUpperInvariant();
        await EnsureCodeUniqueAsync(code, id, ct);
        entity.Update(code, request.Name, ParseType(request.Type), request.Currency,
            request.OpeningBalance, request.BankName, request.AccountNumber, request.AccountHolder, request.IsActive);
        await db.SaveChangesAsync(ct);
        return true;
    }

    public async Task<bool> DeleteAsync(int id, CancellationToken ct = default)
    {
        var entity = await db.CashBankAccounts.FirstOrDefaultAsync(e => e.Id == id, ct);
        if (entity is null) return false;
        db.CashBankAccounts.Remove(entity);
        await db.SaveChangesAsync(ct);
        return true;
    }

    private static CashBankType ParseType(string type) =>
        Enum.TryParse<CashBankType>(type, out var t) ? t : CashBankType.Cash;

    private async Task EnsureCodeUniqueAsync(string code, int? excludeId, CancellationToken ct)
    {
        var exists = await db.CashBankAccounts.AsNoTracking()
            .AnyAsync(e => e.Code == code && (excludeId == null || e.Id != excludeId), ct);
        if (exists)
            throw new ValidationException(
            [
                new FluentValidation.Results.ValidationFailure(
                    nameof(CreateCashBankAccountRequest.Code), $"Code '{code}' is already in use.")
            ]);
    }

    private static CashBankAccountDto ToDto(CashBankAccount x) =>
        new(x.Id, x.Code, x.Name, x.Type.ToString(), x.Currency, x.OpeningBalance,
            x.BankName, x.AccountNumber, x.AccountHolder, x.IsActive, x.CreatedAt, x.CreatedBy);
}
```

- [ ] **Step 5: Register in DI**

In `src/ErpOne.Infrastructure/DependencyInjection.cs`, add using:
```csharp
using ErpOne.Application.CashBank;
```
Add registration after `ICompanySettingService`:
```csharp
        services.AddScoped<ICashBankAccountService, CashBankAccountService>();
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `dotnet test tests/ErpOne.IntegrationTests --filter CashBankAccountServiceTests`
Expected: PASS (4 tests).

- [ ] **Step 7: Commit**

```bash
git add src/ErpOne.Application/CashBank/ src/ErpOne.Infrastructure/Services/CashBankAccountService.cs src/ErpOne.Infrastructure/DependencyInjection.cs tests/ErpOne.IntegrationTests/CashBankAccountServiceTests.cs
git commit -m "feat: add CashBankAccountService with CRUD and tests"
```

---

## Task 3: Web pages + Finance menu group

**Files:**
- Create: `src/ErpOne.Web/Components/Pages/Finance/CashBank/CashBankIndex.razor`
- Create: `src/ErpOne.Web/Components/Pages/Finance/CashBank/CashBankForm.razor`
- Modify: `src/ErpOne.Web/Authorization/AppMenus.cs`
- Modify: `src/ErpOne.Web/Components/_Imports.razor`

**Interfaces:**
- Consumes: `ICashBankAccountService` (Task 2), `ICurrencyService.GetActiveAsync` (existing).

- [ ] **Step 1: Add `Finance` group to AppMenus**

In `src/ErpOne.Web/Authorization/AppMenus.cs`, add a new group (after the `Kasir` group, before `Settings`):
```csharp
        new("Finance",
        [
            new("finance.cash-bank", "Cash & Bank", "bi-bank", CRUD),
        ]),
```
> The data-driven NavMenu renders this automatically (route `/finance/cash-bank` by convention). No NavMenu edit needed.

- [ ] **Step 2: Add the Application.CashBank using to _Imports**

In `src/ErpOne.Web/Components/_Imports.razor`, add after `@using ErpOne.Application.CompanySettings`:
```razor
@using ErpOne.Application.CashBank
```

- [ ] **Step 3: Create the index page (`.pi`)**

`src/ErpOne.Web/Components/Pages/Finance/CashBank/CashBankIndex.razor`:
```razor
@page "/finance/cash-bank"
@attribute [Authorize(Policy = "finance.cash-bank.index")]
@rendermode InteractiveServer
@inject ICashBankAccountService AccountService
@inject NavigationManager Nav
@inject SwalService Swal

<PageTitle>Cash & Bank</PageTitle>

<div class="pi">
    <div class="pi-head">
        <div>
            <nav class="crumbs"><a href="/">Home</a><span class="sep">·</span><span>Finance</span><span class="sep">·</span><span class="here">Cash &amp; Bank</span></nav>
            <h1>Cash &amp; Bank</h1>
            <p>Accounts that money is paid from and received into.</p>
        </div>
        <AuthorizeView Policy="finance.cash-bank.create">
            <Authorized>
                <div class="pi-actions">
                    <a class="btn btn-primary" href="/finance/cash-bank/new"><i class="bi bi-plus-lg"></i> Add account</a>
                </div>
            </Authorized>
        </AuthorizeView>
    </div>

    <div class="toolbar">
        <div class="search">
            <i class="bi bi-search"></i>
            <input placeholder="Search code or name…" @bind="_search" @bind:event="oninput" @onkeyup="OnSearchKeyUp" />
        </div>
    </div>

    @if (_page is null)
    {
        <div class="pi-loading"><span class="spinner-border spinner-border-sm me-2" role="status"></span>Loading…</div>
    }
    else if (_page.Total == 0)
    {
        <div class="empty">
            <div class="empty-ic"><i class="bi bi-bank"></i></div>
            <p>
                @if (string.IsNullOrEmpty(_search))
                {
                    <span>No accounts yet. <a href="/finance/cash-bank/new">Add the first one.</a></span>
                }
                else
                {
                    <span>No accounts match your search.</span>
                }
            </p>
        </div>
    }
    else
    {
        <div class="card">
            <div class="card-top">
                <span class="n">Showing <b>@((_page.Page - 1) * PageSize + 1)–@Math.Min(_page.Page * PageSize, _page.Total)</b> of <b>@_page.Total.ToString("N0")</b></span>
            </div>
            <div class="table-responsive">
                <table>
                    <thead>
                        <tr>
                            <th style="width:120px">Code</th>
                            <th>Name</th>
                            <th style="width:90px">Type</th>
                            <th>Bank / Account</th>
                            <th style="width:90px">Currency</th>
                            <th class="text-end" style="width:150px">Opening</th>
                            <th style="width:80px" class="text-center">Active</th>
                            <th class="text-end pe-3" style="width:120px"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in _page.Items)
                        {
                            <tr>
                                <td class="code mono">@item.Code</td>
                                <td class="nm">@item.Name</td>
                                <td>@item.Type</td>
                                <td>@(item.Type == "Bank" ? $"{item.BankName} · {item.AccountNumber}" : "—")</td>
                                <td class="mono">@item.Currency</td>
                                <td class="text-end mono">@item.OpeningBalance.ToString("N2")</td>
                                <td class="text-center">@(item.IsActive ? "✔" : "—")</td>
                                <td class="text-end pe-3 text-nowrap">
                                    <AuthorizeView Policy="finance.cash-bank.edit">
                                        <Authorized>
                                            <a class="btn btn-sm btn-outline-primary me-1" href="@($"/finance/cash-bank/{item.Id}/edit")" title="Edit"><i class="bi bi-pencil"></i></a>
                                        </Authorized>
                                    </AuthorizeView>
                                    <AuthorizeView Policy="finance.cash-bank.delete">
                                        <Authorized>
                                            <button class="btn btn-sm btn-outline-danger" @onclick="() => DeleteAsync(item.Id, item.Name)" title="Delete"><i class="bi bi-trash3"></i></button>
                                        </Authorized>
                                    </AuthorizeView>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @if (_page.TotalPages > 1)
            {
                <div class="card-foot">
                    <Pager Page="_page.Page" TotalPages="_page.TotalPages" OnPageChanged="GoToPageAsync" />
                </div>
            }
        </div>
    }
</div>

@code {
    private const int PageSize = 15;
    private PagedResult<CashBankAccountDto>? _page;
    private int _currentPage = 1;
    private string? _search;

    protected override async Task OnInitializedAsync() => await LoadAsync();

    private async Task LoadAsync() =>
        _page = await AccountService.GetPagedAsync(_currentPage, PageSize, _search);

    private async Task OnSearchKeyUp(KeyboardEventArgs e)
    {
        _currentPage = 1;
        await LoadAsync();
    }

    private async Task GoToPageAsync(int page)
    {
        _currentPage = page;
        await LoadAsync();
    }

    private async Task DeleteAsync(int id, string name)
    {
        if (!await Swal.ConfirmAsync("Delete account?", $"\"{name}\" will be permanently removed."))
            return;
        await AccountService.DeleteAsync(id);
        await LoadAsync();
        await Swal.ToastAsync("success", "Account deleted");
    }
}
```

- [ ] **Step 4: Create the form page (`.cf`)**

`src/ErpOne.Web/Components/Pages/Finance/CashBank/CashBankForm.razor`:
```razor
@page "/finance/cash-bank/new"
@page "/finance/cash-bank/{Id:int}/edit"
@attribute [Authorize]
@rendermode InteractiveServer
@using FluentValidation
@inject ICashBankAccountService AccountService
@inject ICurrencyService CurrencyService
@inject IAuthorizationService Auth
@inject NavigationManager Nav

<PageTitle>@Title</PageTitle>

<div class="cf">
    <div class="cf-top">
        <div class="crumbs">
            <a href="/finance/cash-bank">Finance</a>
            <i class="bi bi-chevron-right"></i>
            <a href="/finance/cash-bank">Cash &amp; Bank</a>
            <i class="bi bi-chevron-right"></i>
            <span class="here">@(Id is null ? "New" : "Edit")</span>
        </div>
        <h1>@Title</h1>
    </div>

    @if (_loading)
    {
        <div class="text-center py-5"><div class="spinner-border text-primary" role="status"></div></div>
    }
    else if (_notFound)
    {
        <div class="cf-alert warn"><i class="bi bi-exclamation-triangle"></i> Account not found.</div>
    }
    else
    {
        @if (_error is not null)
        {
            <div class="cf-alert err"><i class="bi bi-exclamation-octagon"></i> @_error</div>
        }

        <div class="cf-wrap">
            <section class="card">
                <div class="card-h">
                    <span class="hd-ic"><i class="bi bi-bank"></i></span>
                    <div class="hd-tx">
                        <h2>Account information</h2>
                        <p>A cash or bank account used for payments and receipts.</p>
                    </div>
                </div>
                <div class="card-b">
                    <div class="grid">
                        <div class="f c3">
                            <label class="fl">Code <span class="req">*</span></label>
                            <input class="ctl mono up @(_codeError is not null ? "bad" : "")" placeholder="CASH" maxlength="20" @bind="_code" @bind:event="oninput" />
                            @if (_codeError is not null) { <div class="err-txt">@_codeError</div> }
                        </div>
                        <div class="f c6">
                            <label class="fl">Name <span class="req">*</span></label>
                            <input class="ctl @(_nameError is not null ? "bad" : "")" placeholder="Main Cash" maxlength="100" @bind="_name" @bind:event="oninput" />
                            @if (_nameError is not null) { <div class="err-txt">@_nameError</div> }
                        </div>
                        <div class="f c3">
                            <label class="fl">Type <span class="req">*</span></label>
                            <select class="ctl" @bind="_type">
                                <option value="Cash">Cash</option>
                                <option value="Bank">Bank</option>
                            </select>
                        </div>
                        <div class="f c3">
                            <label class="fl">Currency</label>
                            <select class="ctl" @bind="_currency">
                                @foreach (var c in _currencies)
                                {
                                    <option value="@c.Code">@c.Code — @c.Name</option>
                                }
                            </select>
                        </div>
                        <div class="f c3">
                            <label class="fl">Opening balance</label>
                            <input type="number" step="0.01" min="0" class="ctl mono" @bind="_openingBalance" />
                        </div>
                        @if (_type == "Bank")
                        {
                            <div class="f c6">
                                <label class="fl">Bank name</label>
                                <input class="ctl" maxlength="100" @bind="_bankName" />
                            </div>
                            <div class="f c6">
                                <label class="fl">Account number</label>
                                <input class="ctl mono" maxlength="50" @bind="_accountNumber" />
                            </div>
                            <div class="f c6">
                                <label class="fl">Account holder</label>
                                <input class="ctl" maxlength="100" @bind="_accountHolder" />
                            </div>
                        }
                        <div class="f c12 d-flex align-items-end">
                            <label class="fl-check"><input type="checkbox" @bind="_isActive" /> Active</label>
                        </div>
                    </div>
                </div>
            </section>

            <div class="pf-footer">
                <div class="in">
                    <span class="note"><span class="req">*</span> required fields</span>
                    <a class="btn btn-ghost" href="/finance/cash-bank"><i class="bi bi-x-lg"></i> Cancel</a>
                    <button class="btn btn-primary" @onclick="SaveAsync" disabled="@_saving">
                        @if (_saving) { <span class="spinner-border spinner-border-sm me-1" role="status"></span> }
                        else { <i class="bi bi-check2"></i> }
                        Save account
                    </button>
                </div>
            </div>
        </div>
    }
</div>

@code {
    [Parameter] public int? Id { get; set; }

    private string _code = string.Empty;
    private string _name = string.Empty;
    private string _type = "Cash";
    private string _currency = "IDR";
    private decimal _openingBalance;
    private string? _bankName, _accountNumber, _accountHolder;
    private bool _isActive = true;
    private IReadOnlyList<CurrencyDto> _currencies = [];
    private bool _loading = true, _saving, _notFound;
    private string? _error, _codeError, _nameError;

    [CascadingParameter] private Task<AuthenticationState> AuthStateTask { get; set; } = default!;

    private string Title => Id is null ? "Add Account" : "Edit Account";

    protected override async Task OnInitializedAsync()
    {
        var state = await AuthStateTask;
        var perm = Id is null ? AppMenus.Perm("finance.cash-bank", "create") : AppMenus.Perm("finance.cash-bank", "edit");
        if (!(await Auth.AuthorizeAsync(state.User, perm)).Succeeded)
        {
            Nav.NavigateTo("/finance/cash-bank");
            return;
        }

        _currencies = await CurrencyService.GetActiveAsync();

        if (Id is int id)
        {
            var a = await AccountService.GetByIdAsync(id);
            if (a is null) _notFound = true;
            else
            {
                _code = a.Code; _name = a.Name; _type = a.Type; _currency = a.Currency;
                _openingBalance = a.OpeningBalance; _bankName = a.BankName;
                _accountNumber = a.AccountNumber; _accountHolder = a.AccountHolder; _isActive = a.IsActive;
            }
        }
        _loading = false;
    }

    private async Task SaveAsync()
    {
        _error = _codeError = _nameError = null;
        _saving = true;
        try
        {
            var req = new CreateCashBankAccountRequest(_code, _name, _type, _currency, _openingBalance, _bankName, _accountNumber, _accountHolder, _isActive);
            if (Id is int id)
                await AccountService.UpdateAsync(id, new UpdateCashBankAccountRequest(_code, _name, _type, _currency, _openingBalance, _bankName, _accountNumber, _accountHolder, _isActive));
            else
                await AccountService.CreateAsync(req);

            Nav.NavigateTo("/finance/cash-bank");
        }
        catch (ValidationException ex)
        {
            foreach (var e in ex.Errors)
            {
                if (e.PropertyName == nameof(CreateCashBankAccountRequest.Code)) _codeError = e.ErrorMessage;
                else if (e.PropertyName == nameof(CreateCashBankAccountRequest.Name)) _nameError = e.ErrorMessage;
                else _error = e.ErrorMessage;
            }
        }
        finally { _saving = false; }
    }
}
```

- [ ] **Step 5: Build**

Run: `dotnet build`
Expected: Build succeeded.

- [ ] **Step 6: Commit**

```bash
git add src/ErpOne.Web/Components/Pages/Finance/ src/ErpOne.Web/Authorization/AppMenus.cs src/ErpOne.Web/Components/_Imports.razor
git commit -m "feat: add Cash & Bank pages and Finance menu group"
```

---

## Final verification
- [ ] `dotnet build && dotnet test` → all pass.
- [ ] Apply migration: `dotnet ef database update --project src/ErpOne.Infrastructure --startup-project src/ErpOne.Web`.
- [ ] Smoke test: `/finance/cash-bank` CRUD; Finance group appears in nav (after restart + re-login for permissions); Bank fields show only when Type=Bank.

## Self-review notes
- Mirrors the `Currency` master (proven). Currency stored as string code; no FK.
- `_currencies` uses `CurrencyDto` (already in `_Imports` via `ErpOne.Application.Currencies`).
- New `finance.cash-bank` permissions auto-seed to admin on startup; new Finance nav group auto-renders (data-driven NavMenu).
